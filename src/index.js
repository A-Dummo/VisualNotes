// Implement panning by translating container through css
// If container no longer a container, then should it be an anchor?
// Should there be an actual anchor element which notes're relative to?
const container = document.querySelector("#container");